<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
  </head>
  <body>
    <div class="flex flex-col">
      <!-- NAVBAR -->
      {{>navbar}}

      <main class="max-w-6xl px-4 mx-auto mt-24">
        <h2 class="text-2xl font-bold mb-12 text-center">
          {{title}}
        </h2>
        <div class="mb-8">
          <form class="space-y-6" action="/login" method="POST">
            
            {{#if message.success}}
            <div>
              <p class="text-sm/6 font-medium text-green-500">{{message.success}}</p>
            </div>
            {{/if}}
            
            <div>
              <label for="email" class="block text-sm/6 font-medium text-gray-900">Email address</label>
              <div class="mt-2">
                <input type="email" name="email" id="email" autocomplete="email" required class="block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6">
              </div>
            </div>

            <div>
              <div class="flex items-center justify-between">
                <label for="password" class="block text-sm/6 font-medium text-gray-900">Password</label>
                
              </div>
              <div class="mt-2">
                <input type="password" name="password" id="password" autocomplete="current-password" required class="block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6">
              </div>
            </div>

            {{#if message.error}}
            <div>
              <p class="text-sm/6 font-medium text-red-500">{{message.error}}</p>
            </div>
            {{/if}}
            

            <div>
              <button type="submit" class="flex w-full justify-center rounded-md bg-indigo-600 px-3 py-1.5 text-sm/6 font-semibold text-white shadow-xs hover:bg-indigo-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Login</button>
            </div>

            <p class="mt-2 text-center text-sm/6 text-gray-500">
              Don't have an account? 
              <a href="/register" class="font-semibold text-indigo-600 hover:text-indigo-500">Create Account</a>
            </p>
          </form>
        </div>
      </main>

      <!-- FOOTER -->
      {{>footer}}
    </div>
  </body>
</html>